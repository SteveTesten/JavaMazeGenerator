package mazegen;

import java.lang.Integer;

import mazegen.Cell.WallState;

public class MazeGenerator
{

/*	public static void main(String[] args)
	{
		if (args.length==2 && Integer.parseInt(args[0]) > 0 && Integer.parseInt(args[1]) > 0)
		{
			System.out.println("Maze dimensions: " + args[0] + " by " + args[1]);
			int rows = Integer.parseInt(args[0]);
			int columns = Integer.parseInt(args[1]);
			
		}
		else
			System.out.println("Maze dimensions not entered properly! Try again with args 'x y' where 'x' and 'y' are integers > 0.");
			
	}*/

}
